In the project compilation, there's a directory here called `site`. It contains all the files generated by `mvn site`, used to host project documentation and information. I've added this to .gitignore, so it won't show up in the GitHub repo, because it's just a bunch of auto-generated files.